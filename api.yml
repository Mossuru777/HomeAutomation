openapi: 3.0.0
info:
  version: 0.0.2
  title: HomeAutomation API
paths: {}
servers:
  - url: /api/v1
components:
  schemas:
    Error:
      type: object
      properties:
        status:
          description: HTTPステータスコード
          type: integer
        messages:
          description: エラーメッセージの配列
          type: string
      required:
        - status
        - messages
